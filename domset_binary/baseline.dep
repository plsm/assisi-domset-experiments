# Deployment specification file
# For each casu in the arena, we need to specifiy:
#     - the hostname of the target machine
#     - the controller and additional files to deploy

beearena :
    casu-013 :
        hostname : bbg-005
        user : assisi
        prefix : deploy
        controller: controllers/domset.py
        # The brackets around additional files are necessary,
        # because the deployment tool expects a list!
        extra : []
    casu-014 :
        hostname : bbg-005
        user : assisi
        prefix : deploy
        controller: controllers/domset.py
        # The brackets around additional files are necessary,
        # because the deployment tool expects a list!
        extra : []
    casu-015 :
        hostname : bbg-006
        user : assisi
        prefix : deploy
        controller: controllers/domset.py
        # The brackets around additional files are necessary,
        # because the deployment tool expects a list!
        extra : []
    casu-016 :
        hostname : bbg-004
        user : assisi
        prefix : deploy
        controller: controllers/domset.py
        # The brackets around additional files are necessary,
        # because the deployment tool expects a list!
        extra : []
